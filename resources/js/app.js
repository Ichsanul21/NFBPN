import Alpine from 'alpinejs';

window.Alpine = Alpine;

Alpine.start();

// Logika kondisional form PPDB kini inline via partial
// resources/views/partials/ppdb-logic.blade.php (satu sumber, kebal stale bundle).
